export const BASE_URL = "http://localhost:3000";

// utils/apiPaths.js
export const API_PATHS = {
    AUTH: {
        LOGIN: "/api/login", // Authenticate user & return JWT token
        GET_UTILISATEUR: "/api/utilisateurs", // Get logged-in user details
    }
}